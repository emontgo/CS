<h1>Editing Project</h1>

<%= render 'form', project: @project %>

<div class="btn btn-primary"><%= link_to 'Show', @project %></div>
<div class="btn btn-primary"><%= link_to 'Back', projects_path %></div>
